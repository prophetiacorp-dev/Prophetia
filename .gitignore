# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
firebase-debug.log*
firebase-debug.*.log*

# Firebase cache
.firebase/

# Firebase config

# Uncomment this if you'd like others to create their own Firebase project.
# For a team working on the same Firebase project(s), it is recommended to leave
# it commented so all members can deploy to the same project(s) in .firebaserc.
# .firebaserc

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage

# nyc test coverage
.nyc_output

# Grunt intermediate storage (http://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# Bower dependency directory (https://bower.io/)
bower_components

# node-waf configuration
.lock-wscript

# Compiled binary addons (http://nodejs.org/api/addons.html)
build/Release

# Dependency directories
node_modules/

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity



# dataconnect generated files
.dataconnect

# =========================================================
# PROPHETIA · Private runtime data
# NUNCA subir pedidos, miembros, claves, drops privados ni stock real.
# Producción: las claves deben vivir como variables/secretos del servidor.
# =========================================================

.env
.env.local
.env.production

secrets/
firebase-admin*.json

# Datos reales del servidor
data/orders.json
data/stock-waitlist.json
data/tribe-members.json
data/private-drops.json
data/private-drop-pages.json
data/stock.json

# Tarifas reales si quieres tratar /data como runtime privado
data/shipping-rates.json

# Backups locales
data/*.backup.json
data/*.backup-local.json

# Design source files
design-source/
*.ai
*.psd
*.rar

.vscode/
.aider*
